services:
    web:
        image: ${WEB_IMAGE:-cinema-party/jitsi-web:latest}
        read_only: false

    prosody:
        read_only: false

    jicofo:
        read_only: false

    jvb:
        read_only: false
